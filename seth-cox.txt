    https://codepen.io/Sethcox16/pen/WNbboed?editors=1100
    skldfjdskflj
    1. What is Semantic HTML? The process of providing meaning to your content
    2. What is HTML used for? Standard markup language for creating web pages
    3. What is an attribute and where do we put it? Provide additional information about HTML elements / They are always specficied in the start tag
    4. What is the h1 tag used for? How many times should I use it on a page? H1 tag is usually used for the title of the page, it can be used more then once, but most use h2,etc after using h1 for the title.
    5. Name two tags that have required attributes.  <a href> and <img src>
    6. What do we put in the head of our HTML document? Not sure if you mean <HTML> or using a <header>
    7. What is an id? specifics a unique id for an HTML element
    8. What elements can I add an id to? any HTML element
    9. How many times can I use the same id on a page? Once per page
    10. What is a class? define styles for elements with the same class name
    11. What elements can I add a class to? any HTML element
    12. How many times can I use the same class on a page? as much as you want
    13. How do I get my link to open in a new tab? _blank
    14. What is the alt attribute used for? Specifies an alternative text to be used, if an image cannot be used
    15. How do I reference an id? #
    16. What is the difference between a section and a div? A div defines a division or a section in HTML used to group block elements to format with CSS
    17. What is CSS used for? describes how HTML elements are to be displayed
    18. How to we select an element? Example - every h2 on the page - .h2 {}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one - ID is for a specific section, where classes you can use on multiple
    20. How do we select classes in CSS? .classname {}
    21. How do we select a p element with a single class of “human””? .human {}
    22. What is a parent child selector? When would this be useful? 
    23. How do you select all links within a div with the class of sidebar? #sidebar a {}
    24. What is a pseudo selector? Used when you may not have a set amount of elements, but want to style them differently
    25. What do we use the change the spacing between lines? line-height: X;
    26. What do we use to change the spacing between letters? letter-spacing: 3px;
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform:
    28. How do I add a 1px border around my div that is dotted and black? border: 1px dotted Black;
    29. How do I select everything on the page? .force-select-all (not sure if this was what you are looking for)
    30. How do I write a comment in CSS? //
    31. How do I find out what file I am in, when I am using the command line? git status
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm / when you do this it is deleted forever
    34. Why should I use version control? allows you to keep track of changes made and can revert back to specific versions
    35. How often should I commit to github? often (every 15mins)
    36. What is the command we would use to push our repo up to github? git push -u origin branch-name
    37. Walk me through Lambda's git flow. 

